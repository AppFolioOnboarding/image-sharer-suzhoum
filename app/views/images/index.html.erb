<%= link_to("Link to submit new image", new_image_path) %>

<h1>Listing Images</h1>

  <% @images.each do |image| %>
    <tr>
      <td><%= link_to(image_tag(image.image_url, class: "image-preview"), image_path(Image.find_by(image_url: image.image_url)))%></td>
    </tr>
  <% end %>
</table>
